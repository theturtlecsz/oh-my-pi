Bounded access to the owner's work ledger (worlds→surfaces→promises→work items). {{sequence}} Reads are free: get_work, tree, waiting, my_now, status. append_evidence writes immediately; kind is required — every receipt is typed: {{kinds}}. create_work, queue_work, revise_work, set_now, request_closeout, record_health, and cancel_work are owner-confirmed two-phase writes: the first call previews and writes nothing; repeat with confirm:true plus the confirmation_id from the preview only after Chris says yes. During user-invoked intake, the first confirmed work item or batch parent becomes NOW in the same previewed operation. create_work queue:true and queue_work add {{queueNoun}}. A batch publishes one parent plus children with native parent/block relations behind one preview; partial failure reports the exact landed inventory. Writes from a scoped git repo require its {{markerFile}} target unless project is explicit. Never assume success without success:true. Only owner-entered /done closes work. request_closeout remains /summary's asynchronous recommendation. record_health, request_closeout, and cancel_work require an owner-entered /summary or /done; a keep-open verdict blocks them.
