Bounded access to the owner's work ledger (worlds→surfaces→promises→work items). {{sequence}} Reads are free: get_work, tree, waiting, my_now, status, list_work. list_work is a free bounded read — project required — listing that project's open items one per line as KEY | STATE | TITLE. append_evidence and run_audit write immediately; append_evidence kind is required — every receipt is typed: {{kinds}}. run_audit runs the native acceptance audit for the live attempt on the current NOW item. create_work, queue_work, revise_work, set_now, record_health, and cancel_work are two-phase writes: the first call previews and writes nothing; the write lands only when the identical payload is repeated with confirm:true plus the matching confirmation_id from the preview. Routine bookkeeping self-confirms (OMP-23): for create_work filing a stray finding/capture, revise_work recording an already-decided bookkeeping correction, and record_health, validate that the preview matches the intended payload and confirm in the same turn without showing Chris. Formal intake publication, queue_work, set_now, and cancel_work stay owner-confirmed: wait for Chris's explicit yes before confirming. During user-invoked intake, the first confirmed work item or batch parent becomes NOW in the same previewed operation. create_work queue:true and queue_work add {{queueNoun}} and require question (a single-line owner decision question, max 240 chars). A batch publishes one parent plus children with native parent/block relations behind one preview; partial failure reports the exact landed inventory. Writes from a scoped git repo require its {{markerFile}} target unless project is explicit. Never assume success without success:true. Only owner-entered /done closes work. record_health is status-only and refuses body. record_health and cancel_work require an owner-entered /summary or /done; a keep-open verdict blocks them.
