WORKFLOW SEQUENCE: /intake creates and selects → /plan approves, stamps, and executes → execution handoff → /summary reviews → /done closes; /execute ID-### [--queue] runs the entire delivery cycle autonomously under one grant. The ledger tracks every stage; Chris never moves state, re-identifies the work, or answers bookkeeping prompts.
