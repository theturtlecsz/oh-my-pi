AUDIT GATE (HOME-131, OMP-38) — this /summary cannot settle until an independent audit has run and its report is on the record.
Before Phase 5's review comment:
1. Read the bound packet from the ledger: call the `work` tool with action:"get_work" on the reviewed item. Its PLAN PACKET carries the exact approved plan body, the acceptance criteria, the plan receipt SHA-256, the approved plan SHA-256, and the finalized candidate's id/sha256/Final commit. Build the audit input from THESE values only — never from transcript scrollback or memory. If the packet is missing, unfinalized, or capped, stop and fix that first (/plan to stamp, /summary to freeze + finalize); the gate refuses auditor spawns until the packet and binding agree.
2. Spawn EXACTLY ONE `auditor` task (fresh context, blocking). Its task text must inline five labeled sections, each carrying the actual content — never a pointer like "attached below": Approved plan (the packet's exact plan body, plus one line `Plan receipt SHA-256: <the packet's plan receipt sha256>` — the gate requires this citation), Acceptance criteria (the packet's criteria with AC-IDs), Starting state (starting commit + pre-existing dirty files), Final diff, Verification (exact commands + results). The Final diff section carries either the inline git diff itself plus one line `Final commit: <the packet's final commit>`, or — for large or binary committed work — a complete git manifest with exactly these fields: `Mode: git-range-sha256`, `Repository:` absolute repository path, `Start commit:` 40-hex commit, `Final commit:` 40-hex commit, `SHA-256:` 64-hex digest of `git -C REPOSITORY diff --binary --full-index START..FINAL --`. Either way the Final commit line must name the packet's finalized candidate commit exactly — the gate refuses mismatches before spawn. The auditor reconstructs and hash-verifies the manifest diff itself; a partial manifest is refused before spawn. Do NOT include your own self-assessment, conclusions, or confidence claims — the auditor inspects the work cold. NEVER pass outputSchema on the auditor task: schema serialization mangles the plain-text report and the gate rejects schema-bearing spawns.
3. Wait for its report: PLAIN headed text — first line VERDICT: PASS | NEEDS_FIX | BLOCKED, then FINDINGS (ordered by severity with AC-ID/file:line/evidence/impact/minimal fix), ACCEPTANCE COVERAGE, OUT OF SCOPE, CHECKS RUN, REMAINING QUESTIONS. If the gate refuses a report it says so explicitly with the missing parts — a refusal means the audit RAN but its report was unusable; exactly ONE fresh replacement auditor is allowed per /summary attempt. A second unusable report exhausts the attempt: this /summary ends blocked without an audit receipt, and the next owner-entered /summary starts a fresh bounded attempt.
4. Forward the report VERBATIM as the body of a `work` tool call, action:"append_evidence", kind:"audit" — the host records the receipt-bound bytes, never an edited copy. Never paraphrase, trim, or soften it. Owner-facing chat stays plain-language; the verbatim report lives on the ledger item.
A NEEDS_FIX verdict still gets forwarded — it ends this summary attempt; fixes happen afterward and the next owner-entered /summary starts a fresh auditor. Never continue a prior auditor's session. If the candidate is re-frozen after the auditor ran, its report is stale: the gate refuses it and only a fresh /summary + fresh auditor can produce a recordable audit.
