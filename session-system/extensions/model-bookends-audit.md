AUDIT GATE (HOME-131, OMP-47) — this /summary cannot settle until the ledger-owned close attempt has an accepted audit report on the record.
The Work Ledger service owns EVERY gate: the attempt, the sealed task, the launch budget (3 launches, 2 accepted reports), report normalization, and the audit receipt. You only transport.
1. After verification evidence is appended, the service seals the audit manifest. Call the `work` tool with action:"get_work" on the reviewed item and read its AUDIT TASK section — the complete five-section auditor task, sealed byte-for-byte. If it says none is sealed, fix that first (/plan to stamp, /summary to begin the attempt, append verification evidence); nothing else unblocks it.
2. Spawn EXACTLY ONE `auditor` task (fresh context, blocking) whose task text is EXACTLY the sealed body between the BEGIN/END markers — byte-for-byte, no trimming, no rewrapping, nothing added. Never pass outputSchema. The gate reserves one bounded launch against the sealed hash before the spawn; changed bytes are refused with zero slot burn.
3. The auditor reports PLAIN headed text — first line VERDICT: PASS | NEEDS_FIX | BLOCKED, then FINDINGS, ACCEPTANCE COVERAGE, OUT OF SCOPE, CHECKS RUN, REMAINING QUESTIONS. Its transport result settles the launch automatically: the service normalizes the payload, mints the audit receipt itself on an accepted report, and answers with a typed event naming the outcome, the remaining budget, and the legal next actions. Follow THAT event — never re-litigate a refusal, never spawn a replacement unless the event says one is legal.
4. There is nothing to forward: audit receipts are service-minted; an append_evidence kind:"audit" is always refused. After a PASS, append the closeout review (kind:"closeout"), which atomically records the review and requests closeout once its checkpoint is delivered. A NEEDS_FIX or BLOCKED verdict ends this attempt; fixes happen afterward and the next owner-entered /summary starts a fresh bounded attempt.
