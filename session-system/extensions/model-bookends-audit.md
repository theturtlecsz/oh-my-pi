AUDIT GATE (HOME-131) — this /summary cannot settle until an independent audit has run and its report is on the record.
Before Phase 5's review comment:
1. Spawn EXACTLY ONE `auditor` task (fresh context, blocking). Its task text must inline five labeled sections, each carrying the actual content — never a pointer like "attached below": Approved plan (the plan text), Acceptance criteria (with AC-IDs), Starting state (starting commit + pre-existing dirty files), Final diff (the diff itself), Verification (exact commands + results). Do NOT include your own self-assessment, conclusions, or confidence claims — the auditor inspects the work cold. NEVER pass outputSchema on the auditor task: schema serialization mangles the plain-text report and the gate rejects schema-bearing spawns.
2. Wait for its report: PLAIN headed text — first line VERDICT: PASS | NEEDS_FIX | BLOCKED, then FINDINGS (ordered by severity with AC-ID/file:line/evidence/impact/minimal fix), ACCEPTANCE COVERAGE, OUT OF SCOPE, CHECKS RUN, REMAINING QUESTIONS. If the gate refuses a report it says so explicitly with the missing parts — a refusal means the audit RAN but its report was unusable; spawn one fresh auditor with a corrected contract instead of treating it as "no audit".
3. Forward the report VERBATIM as the body of a `work` tool call, action:"append_evidence", kind:"audit" — the host records the receipt-bound bytes, never an edited copy. Never paraphrase, trim, or soften it. Owner-facing chat stays plain-language; the verbatim report lives on the ledger item.
A NEEDS_FIX verdict still gets forwarded — it ends this summary attempt; fixes happen afterward and the next owner-entered /summary starts a fresh auditor. Never continue a prior auditor's session. If an auditor run fails or returns a structurally unusable report, spawn one fresh replacement auditor; after a usable report is on record, never spawn another auditor in that summary attempt.
