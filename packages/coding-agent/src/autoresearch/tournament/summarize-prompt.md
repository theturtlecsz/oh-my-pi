Summarize the following hypothesis write-up so that the resulting summary is strictly under {{maxChars}} characters.
Preserve all key claims, mechanisms, and reasoning faithfully.
Do not introduce external claims or commentary. Output only the summarized text.

Write-up:
{{writeUp}}
