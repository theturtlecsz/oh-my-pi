# Goal

You are the Codex lead responsible for the OMP programme handoff. Produce one comprehensive, evidence-grounded implementation handoff that translates the attached **OMP_Product_Platform_Review_2026-09-18.md** into executable work within the existing programme, using native Codex goal tracking and durable checkpoints while you work. This brief replaces the earlier handoff-generator prompt; its historical filename does not require Astra as the default model.

The deliverable is **OMP_Astra_Implementation_Handoff.md**. Write and maintain this document, including its working checkpoint; do not begin OMP implementation. It must preserve the intended product and prior obligations, establish the actual starting state, define economical development practices, and make the first bounded work packet executable once its prerequisites are satisfied.

# Context (carry forward)

- OMP is a personal engineering and research platform centred on https://github.com/theturtlecsz/oh-my-pi and https://github.com/theturtlecsz/omp-webui. A commercial SaaS business is not assumed.
- Preserve substantial ECC reuse; useful neurosymbolic interpretation, exploration, constraints and evidence; autonomous engineering; fleet knowledge and correction; full adaptive autoresearch; recursive improvement of research methods and executable platform mechanisms; and a coherent user control surface.
- Predictable, standardized **user interactions** do not require deterministic internal intelligence, restricted worker collaboration, or a fixed research pipeline. Architectural styles, fleet terminology, vendors, and model roles must justify their practical benefits.
- Optimize total cost per useful outcome: owner intake, supervision, review, integration, recovery, failed work, model and compute usage, subscriptions, and ongoing maintenance. Keep human time, cash/quota, throughput, and research quality separate.
- The review's central recommendation is to preserve the ambition while delivering usable journeys sooner. Its verdict was: coherent purpose, partially coherent roadmap, substantial implementation, but insufficient proof of integrated usefulness and economics.
- The owner currently uses GPT-5.6 Medium as orchestrator, Fable 5.1 as planner, Gemini 3.8 Flash as implementer, and Kimi as reviewer to conserve Codex credits. The owner explicitly authorizes redesigning this external development workflow. Apply the selective routing below without asking again whether every model must participate. This authorization does not amend OMP's production execution/audit rules or authorize live operations.
- Treat other review recommendations as hypotheses to reconcile with current evidence. A proposed product-policy amendment is not automatically an accepted obligation.

# Codex operation and durable continuity

Use these practices during handoff preparation and specify their implementation equivalent in the handoff.

1. **Establish the native goal.** If this was launched with `/goal`, verify that the active objective is producing this handoff. Otherwise use an actually available native goal tool to set it. Keep goal text short and reference this file; the documented CLI limit is 4,000 characters. Do not paste the whole programme into the goal, simulate activation with a heading, or overwrite an unrelated active goal. If unavailable, report that specific limitation, provide the supported launch instruction, and continue permitted document work with file checkpoints. Never create a replacement goal engine.

2. **Check the real client.** Identify CLI/desktop/IDE, version, current model/effort, workspace, applicable instructions, and available native goal, plan, resume, compaction, and hook capabilities. Verify actual provider adapters and billing/authentication surfaces without displaying secrets. Use current official documentation for unsupported or uncertain details. Do not treat a provider model's existence as proof this client can launch it.

3. **Maintain one working plan.** Use Codex's native planning facility where available. Keep the current drafting checkpoint in this handoff, not solely in chat or a tool plan. For later implementation, select the existing authoritative execution-plan/checkpoint location and reuse it. Keep MASTER as the programme index and WorkService as the work/effect authority where applicable. Link their current records rather than duplicate them.

4. **Checkpoint meaningful progress.** After verified milestones, material decisions, or blockers, atomically update a compact Progress/Resume section. Record objective and criteria; repository/worktree/branch/commit plus uncommitted work; completed evidence; failed approaches; controlling decisions; remaining work and exact next action; active worker/run IDs and unresolved effects; applicable authority; budget usage/unknowns; source versions and timestamp. Preserve completed artifacts, source inventory, and obligation coverage progressively rather than relying on a final summary. Update before a deliberate pause or compaction when possible; do not rely on predicting automatic compaction.

5. **Restore before continuing.** After compaction, reconnect, restart, or a fresh session, read the checkpoint, applicable AGENTS.md guidance, and the sources needed for the next packet. Reconcile them with actual code, tests, running jobs, and native records. Preserve completed work, reconcile uncertain effects before relaunch, and never infer a grant from notes. Resume the same task/session when appropriate; fresh sessions must also recover from files. Do not rewrite accepted decisions from memory.

6. **Use a small entry point.** Specify a concise AGENTS.md addition pointing to programme authority, the active checkpoint, verification commands, and this recovery procedure. Keep complete requirements in referenced files. For this authoring task, include the proposed addition in the handoff; do not edit repository instructions.

7. **Use hooks proportionately.** Reuse existing supported hooks. If automatic restoration is missing, propose one small, deterministic SessionStart hook for startup/resume/compact that injects bounded checkpoint context and source pointers. Validate against the installed hook schema. It must not call another model, parse unstable transcript formats, alter grants, or create another work ledger. Include its proposed configuration, fallback, and verification in the handoff; installation is future work. Native compaction and optional memories remain aids, not the authoritative requirements store.

8. **Prove continuity.** Define observable checks for manual compaction, resumed sessions, and a fresh session using only durable files. Each must recover the same objective, constraints, evidence, remaining work, and next action without owner reconstruction or duplicate execution. Record which checks actually ran. Do not claim automatic-compaction or fresh-session success from document inspection. Run only safely supported authoring checks now; leave unavailable or implementation-dependent checks explicitly unverified.

# Economical model routing

**Assessment:** The four-model pool is reasonable, but mandatory planner → implementer → reviewer → orchestrator passes can spend the savings on repeated context loading, plan translation, supervision, and repair. No inspected evidence establishes that this sequence is optimal for OMP. Change when roles are invoked before adding providers or replacing every model.

Use this starting policy, subject to the actual available bindings:

| Work | Default |
|---|---|
| Goal ownership, routine planning, integration, small direct fixes | Existing GPT-5.6 Medium lead; verify its exact model binding. Sol Medium is the intended baseline if the user's “GPT-5.6” resolves to Sol. Keep one accountable lead. |
| Bounded implementation with executable feedback | Gemini 3.8 Flash through the already configured, authorized adapter. Let it inspect relevant files, implement, test, and perform bounded repair within one packet. |
| Novel architecture, ambiguous cross-system contracts, or failed ordinary planning | One focused Fable 5.1 consultation with explicit questions and a concrete decision/plan artifact. Reuse the resulting plan until evidence invalidates it. |
| Independent review of meaningful changes | Existing Kimi binding, in a separate context, examining the actual candidate, requirements, and evidence. Verify the exact model; do not infer it from the brand name. |
| Exceptional unresolved architecture or repeated capability failure | One bounded stronger-model escalation, potentially Astra if already available. Return to the normal lead afterward; do not make Astra an always-running supervisor. |

For tiny, well-understood changes, the lead can work directly when delegating would cost more. For security, authority, schema, recovery, evaluator, and cross-repository changes, require independent review at the appropriate integrated boundary. Preserve mandatory native acceptance/audit obligations; external Kimi review does not replace a separately required OMP auditor. For low-risk work, batch review at a meaningful change boundary instead of sending every edit through the full cast.

Allow helpers to collaborate when useful. Begin with one active implementation packet; permit at most two helpers concurrently when work is demonstrably independent and integration ownership is explicit. Use isolated worktrees for concurrent writes. Do not start recursive delegation, Best-of-N, or multiple competing planners by default in this external development workflow. Retain OMP's intended Best-of-N and research capabilities and policies.

Each delegated packet needs the objective, relevant source references, immutable base, allowed files/effects, acceptance checks, applicable limits, and return contract. Require candidate/diff identity, actual checks and results, unresolved findings, usage where available, and concise integration notes. Do not shuttle entire conversation histories. The lead inspects the evidence and integrates; it should not independently redo every completed planning or review pass.

After two unsuccessful repair attempts on the same failure, diagnose and change the packet, route, or assumption rather than repeat the loop. This is a reassessment trigger, not a ban on evidence-driven investigation. Continue independent in-scope work and ask the owner only for a material unresolved decision.

Use real tools/processes for other providers. Do not pretend native Codex subagents can select Fable, Gemini, or Kimi without an actual supported integration, or role-play their independent reviews in one response. If a helper is unavailable, use an already authorized capable route and disclose the substitution; never silently bypass required independent acceptance. Do not build new provider bridges during handoff preparation.

**Cost control:** Minimize verified Codex allowance/credit use per accepted outcome, subject to quality and reasonable total cost. Track external subscription/API use, helper setup, root-model input/output where exposed, retries, review, integration, recovery, elapsed time, and owner minutes separately. API list prices do not establish subscription cost, and external subscription access does not imply API entitlement. Label estimates, unavailable readings, and unattributable shared-account usage. Do not claim wall-clock waiting alone consumes credits; avoid repeated model-driven status polling and use supported waits/completion events.

Use the lowest effort that succeeds on the packet; do not force high/maximum effort everywhere. Preserve stable instructions and load relevant sources incrementally. Respect existing quotas and spending limits; text instructions are not proof of a hard budget cap. Do not buy credits or add paid routes automatically.

Specify a small future matched comparison of the current four-role workflow, this selective workflow, and Codex-only execution on representative tasks. Count failed attempts and maintenance. Retain delegation only where verified quality, owner time, and total economics justify it. Consider Terra/Luna later for qualified bounded work, not an unmeasured wholesale downgrade of programme ownership. Do not run a separate paid benchmark during this handoff task.

# Sources and inspection scope

Use the attached review as the synthesis to translate, available conversation decisions as context, and current repository/native evidence to establish implementation status. Read applicable repository instructions before inspection.

Inspect both repositories, including the programme branch `codex/omp-programme-20260917` if it still exists. The review inspected OMP main `1b78b801edebc0ce8c4fbd90c8a192e6d5b6bec1`, programme head `46068d9f95ee5fe3faee8599a21a9a0f0d8aa76b`, and WebUI `08a277a542adb6217ba5e6013ae34d5af10f3fff`. These are historical anchors, not assertions about today's heads or installed release.

Reconcile:
- `MASTER.md`, `CONTEXT.md`, `CURRENT-HANDOFF.md`, and `IMPLEMENTATION-HANDOFF.md`.
- `docs/programme/ACCEPTED-DECISIONS.md`, `GOAL-PROMPT.md`, `ORIGINAL-OBLIGATIONS.md`, `SCOPE-PRESERVATION.md`, and `REVIEW-SUMMARY.md`.
- The full `docs/programme/ECC-IMPLEMENTATION-SPEC.md` and `AUTORESEARCH-IMPLEMENTATION-PLAN.md`.
- Relevant ADRs, intake/03B exploration, stabilization, production readiness, installation isolation, Fleet Knowledge, cost-control, Harbor/evaluation, reliability, and WebUI plans.

Follow essential references, including older programme obligations. If files moved, locate their successors. Preserve full ECC and autoresearch contracts as authoritative references; your handoff must not silently replace them with a reduced summary.

Inspect code, tests, CI, and already accessible installed/native records where they resolve consequential claims. Use targeted reads and existing evidence; do not launch paid experiments or qualification workloads for this authoring task. Bound inaccessible evidence explicitly and continue useful preparation. Do not invent issue states, commands, test results, deployment success, source citations, or permissions.

# Scope, constraints, and approval boundaries

Create/update only the requested handoff artifact, including its working checkpoint, in an appropriate document location. Using native goal/plan controls for this authoring task is in scope. Keep product repositories, backlog, operational policy, grants, deployments, running work, and global client configuration unchanged. Do not commit, push, open PRs, message others, or perform recovery/cutover actions. Proposed AGENTS.md, hook, configuration, and production-policy changes belong in the handoff as concrete future work.

Separate **accepted obligation**, **review recommendation**, **proposed amendment**, and **currently authorized operation**. For every conflict, identify the controlling source and propose a concrete resolution. Preserve existing obligations until an applicable approved amendment supersedes them; do not request approval again where documented authority already suffices.

The latest owner instruction authorizes the external Codex model-routing simplification above and supersedes earlier mandatory four-role development recipes within that scope. Do not ask for that decision again. Separately identify any proposed changes to OMP production inference retries, Best-of-N defaults, native model/audit hierarchy, UI/research sequencing, or standing intake exploration; do not activate those product-policy amendments through this prompt. Verify the current Q36 state: the review recorded its live recovery/execution authority as separately withheld. This prompt supplies no new Q36 authority. Block only dependent execution, not independent handoff preparation.

Use existing programme identities and owners. This document is an implementation brief and crosswalk, not another parallel programme, work authority, or hand-maintained status ledger. Recommend how it will point to authoritative current state rather than become another stale “current” handoff.

# Required handoff

Lead with decisions and the next useful action. Use the following sections, concise reasoning, evidence links, and tables where they clarify mappings.

## 1. Implementer brief

State the product purpose, the three intended user outcomes, what changes in delivery strategy, what remains protected, and the strongest credible 10× hypothesis.

Describe the simplest credible baseline: a capable coding agent with selected ECC methods, Git/CI, a basic experiment loop, and a small evidence store. Explain what OMP must demonstrate beyond it. Do not present 10× as measured or multiply unrelated gains.

## 2. Starting state and evidence ledger

Record inspected branches/commits, relevant installed identity if available, source dates, divergences, and access limits.

For each major capability, distinguish desired, specified, implemented, tested, deployed, and demonstrated useful. Mark gaps as missing design, implementation, integration, operational qualification, or empirical validation. A document, merged change, model PASS, and passing component test are different evidence.

Revalidate these consequential review findings, documenting what remains true and what newer evidence changes:
- Candidate-branch knowledge and budget work was ahead of main, while installation, native acceptance, accounting integration, ECC activation, and full research remained open.
- Main CI's 121 PostgreSQL and 22 installed recovery tests were meaningful evidence, but did not establish full-program qualification or live usefulness.
- The context compiler used a stage-name query, limited retrieval to ten results, and sorted by fact ID, preventing the documented reranker order from governing the compiled bundle.
- Configured provider-observation routes lacked authoritative outcome lookup in the inspected implementation, potentially blocking uncertain requests indefinitely.
- The native autoresearch logger accepted agent-supplied metrics/status; trusted managed research needs independent candidate-bound measurements.
- WebUI CI exercised upstream OMP rather than the pinned fork; some unavailable-stub paths returned successfully, and browser coverage was limited.
- Native role profiles, audit policy, external development policy, and historical handoffs described different model recipes.
- The four-role hello.txt journey was limited transport/composition evidence, not proof of useful unattended engineering.

Cite source locations and actual receipts; retain uncertainty where evidence is unavailable.

## 3. Complete obligation and disposition crosswalk

Map the review into existing SQL/H/CPK/NSI/FLEET work, 03B and OMP-267–270, ECC WP1–WP8 including WP7A/B, FK-0–FK-7, cost PR1–PR3, relevant stabilization/reliability and Harbor work, OWEB/WFM/desktop, and research R00–R19.

For each substantive obligation show:
**source and existing owner/ID → user outcome → current evidence → keep/simplify/combine/defer/replace/remove → proposed delivery → dependencies → acceptance evidence → retirement or retained destination**.

Do not equate an old ID with current backlog state. Every deferred ambition needs a re-entry gate; every proposed removal must name the lost capability and require an explicit scope decision. Preserve substantive requirements and essential references without copying entire historical plans into every packet.

## 4. Architecture and contract decisions

Specify the smallest adequate implementation of each needed boundary using current modules/APIs where verified. Cover:
- WorkService work/effect authority, Git source identity, session history/replay, and UI projections; avoid duplicate authorities while preserving legitimate local stores.
- One shared admission/job/recovery substrate with workload-specific execution and adaptive research above it.
- Neurosymbolic interpretation, evidence, constraints, ambiguity resolution, ratification, and useful exploration.
- ECC pinned mirror, adapter activation semantics, selected task packs, protected local ownership, updates, removal, and rollback.
- Independent acceptance and research evaluation bound to exact candidate/input/evaluator versions.
- Task-relevant context, source identity, scoped learning, withdrawal/correction, and affected-decision traceability.
- One effective cost/model policy, reservation-to-consumption accounting, unknown usage, allocation between delivery/research/meta-research, and proportionate execution recipes.
- Stable inspect/pause/stop/resume semantics and the distinction between local workbench access and remote supervisory authority.
- Composition of parallel changes: explicit integration responsibility, exact combined baseline, migration compatibility, and acceptance against the integrated candidate.

Separate recommended pure-inference retries with conservative accounting from isolated execution retries and consequential external-effect reconciliation. Preserve collaboration and evolving hypothesis/search graphs within authorized boundaries.

For each architectural change include benefit, tradeoff, simplest adequate alternative, migration path, upstream/operational burden, compatibility/rollback conditions, and the old mechanism actually retired. Retain required Observer/Advisor/audit coverage until equivalent replacement is demonstrated.

## 5. Corrected delivery sequence

Reconcile existing packages into these first three useful deliveries:
1. A real bounded media-discovery goal becomes a reviewed, independently supported change through the installed native path, with usable controls, recovery, and complete owner-time/resource records.
2. A reproducible adaptive research campaign investigates alternatives, revises a hypothesis, retains a failed candidate, evaluates independently, survives interruption, and produces a useful change or defensible conclusion.
3. A two-repository learning/correction journey proves that a supported lesson helps fresh work, counterevidence changes its applicability, and subsequent decisions respect the correction.

For each define its user-visible result, minimum dependencies, owned work packets, likely implementation locations, observable acceptance, failure/recovery cases, release identity, and go/no-go evidence. Make Delivery 1 useful beyond infrastructure scaffolding.

Carry the sequence through literature/ML/simulation, executable mechanism research, broader runtime improvement, continuous/distributed campaigns, full UI, and recursive research. Preserve the full ambition while deferring breadth that does not unblock the next outcome.

Give effort ranges only with assumptions and uncertainty. Re-estimate remaining work from verified state; historical estimates are not deadlines or measured agent velocity.

## 6. Qualification and economics

Retain the distinct accepted gates: 20 consecutive accepted ordinary tasks without unplanned workflow repair, and the separate 72-hour frozen-release mixed workload with at least 30 distinct tasks and 100 actual experiment attempts. Read and retain the full coverage, recovery, integrity, responsiveness, and evidence rules from their source; do not turn polling or planning into trials.

Separate these adoption gates from statistical evidence for a 10× claim. Define matched baselines, quality floors, false-acceptance checks, fresh/held-out tasks, reproduction, the full failure denominator, active human minutes, cash/quota/compute, and maintenance amortization.

Specify cheap falsification experiments for durable goal ownership, useful context/correction, adaptive research, and executable self-improvement. Include stop conditions, protected evaluator independence, and resource allocation. Recommend only validation that resolves an actual contract risk or required gate.

## 7. Exact first bounded work packet

Make the next action concrete enough to execute after its prerequisites are met:
- Outcome and connection to the selected media-discovery problem.
- Existing programme owner/ID and exact starting composition.
- In-scope changes and explicit exclusions.
- Dependencies, current authority, and any unresolved decision.
- Applicable modules/contracts and verified commands, or clearly labelled commands needing confirmation.
- Acceptance observations, evidence artifacts, recovery/rollback, and stopping conditions.

If the precise task, repository, installed manifest, or authority is unavailable, identify that specific input and finish all independent preparation. Do not invent a task or make the whole handoff depend on a broad permission request.

## 8. Codex execution contract and decisions

Incorporate the operating and routing requirements above into the handoff as a concrete contract, not generic advice. Include the verified capability/billing matrix; selected existing checkpoint location and schema; proposed AGENTS.md/hook changes; recovery procedure and test evidence; role triggers and fallback routes; cost measurements; and a short launch/resume runbook for the actual client.

Provide a copyable native /goal invocation for the first implementation packet, pointing to its durable plan and evidence criteria. Label it for later use once prerequisites and applicable authority are satisfied; do not activate it now. Keep later delivery goals attached to the retained full-programme obligations.


End with a short prioritized list: preserve, change, validate next, stop doing now, and the single most valuable next action.

List only owner questions that materially change the plan, with a provisional recommendation, the consequence of choosing differently, and the work actually blocked. Do not re-interview settled Q1–Q35 preferences.

Include a compact source index and a continuation record stating verified facts, remaining uncertainties, the next work packet, and where authoritative status lives.

# Done

Before delivering, verify that every consequential review recommendation and every retained programme family has a traceable disposition; no explicit ambition vanished; the newly authorized external workflow is distinguished from proposed product-policy changes; milestones require user outcomes; and operational claims are supported at the stated maturity. Report whether native goal activation and each continuity check were observed, unavailable, or deferred. A goal marked complete is not evidence that implementation or continuity tests passed.

Provide the handoff file and a brief message naming the first work packet, the chosen model-routing changes and rationale, material changes caused by newer evidence, and only genuinely blocking decisions. Leave the checkpoint coherent and the authoring goal accurately completed or blocked. Do not spin an empty continuation loop or automatically roll into implementation. Stop after producing the document.

# Official capability references

These sources establish documented capabilities, not this installation's configuration or measured OMP economics. Recheck relevant details against the actual client when applying them.

- Native goals, bounded objectives, checkpoints: [Follow a goal](https://learn.chatgpt.com/use-cases/follow-goals).
- Goal text limit, resume, and compaction: [Developer commands](https://learn.chatgpt.com/docs/developer-commands?surface=cli).
- Persistent project guidance: [AGENTS.md](https://learn.chatgpt.com/docs/agent-configuration/agents-md).
- SessionStart restoration: [Hooks](https://learn.chatgpt.com/docs/hooks).
- Model/effort controls and client-dependent availability: [Codex models](https://learn.chatgpt.com/docs/models).
- Allowance depends on workload, context, reasoning, tools, and caching: [Codex pricing](https://learn.chatgpt.com/docs/pricing).
- Fable 5.1 is positioned for demanding reasoning and long-horizon work: [Claude models](https://platform.claude.com/docs/en/models/overview).
- Gemini 3.8 Flash supports agentic coding; higher effort can increase token use: [Google's release guidance](https://blog.google/innovation-and-ai/models-and-research/gemini-models/3-8-flash-and-3-8-flash-cyber/).
- Multiple Kimi model offerings make exact binding relevant: [Kimi platform](https://platform.kimi.ai/).
